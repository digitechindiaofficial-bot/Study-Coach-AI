I want to improve my onboarding flow 
when a new user signs up on GovtGuru.

Currently onboarding only asks for 
target exam. I want to add:
1. Full Name
2. Phone Number with OTP verification
3. Target Exam (already exists)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETE ONBOARDING FLOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1 — Personal Details Screen:

Show this as first onboarding step:

┌─────────────────────────────────┐
│     Welcome to GovtGuru! 🎓    │
│   Let's set up your profile    │
│                                 │
│  Full Name *                   │
│  ┌─────────────────────────┐   │
│  │ Enter your full name    │   │
│  └─────────────────────────┘   │
│                                 │
│  Mobile Number *               │
│  ┌────┐ ┌───────────────────┐  │
│  │ +91│ │ 10 digit number   │  │
│  └────┘ └───────────────────┘  │
│                                 │
│  [Send OTP]                    │
└─────────────────────────────────┘

Validation:
- Name: minimum 2 characters
- Phone: exactly 10 digits
- Phone: starts with 6,7,8,9 only
- Both fields required

STEP 2 — OTP Verification Screen:

After clicking "Send OTP":

┌─────────────────────────────────┐
│      Verify Your Number 📱     │
│                                 │
│  OTP sent to +91-XXXXXX7890    │
│                                 │
│  Enter 6-digit OTP             │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐│
│  │  │ │  │ │  │ │  │ │  │ │  ││
│  └──┘ └──┘ └──┘ └──┘ └──┘ └──┘│
│                                 │
│  [Verify OTP]                  │
│                                 │
│  Didn't receive? Resend in 30s │
└─────────────────────────────────┘

STEP 3 — Target Exam Screen:
(Already exists — keep as is)

┌─────────────────────────────────┐
│    Choose Your Target Exam 🎯  │
│                                 │
│  ○ BPSC                        │
│  ○ UPPSC                       │
│  ○ SSC CGL                     │
│  ○ IBPS PO                     │
│  ... etc from exams table      │
│                                 │
│  Exam Date:                    │
│  [Date Picker]                 │
│                                 │
│  Daily Study Hours:            │
│  [Slider: 1h - 8h]             │
│                                 │
│  [Complete Setup →]            │
└─────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OTP IMPLEMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Use Twilio OR Fast2SMS for OTP 
(Fast2SMS is cheapest for India):

OPTION A — Fast2SMS (Recommended):
Cost: ₹0.25 per OTP (very cheap)
Sign up: fast2sms.com

Add to Replit Secrets:
FAST2SMS_API_KEY = your_api_key

Backend API route:
POST /api/otp/send

app.post('/api/otp/send', async (req, res) => {
  const { phone } = req.body
  
  // Validate phone
  if (!/^[6-9]\d{9}$/.test(phone)) {
    return res.status(400).json({ 
      error: 'Invalid phone number' 
    })
  }
  
  // Generate 6 digit OTP
  const otp = Math.floor(
    100000 + Math.random() * 900000
  ).toString()
  
  // Store OTP in database with expiry
  await supabase
    .from('otp_verifications')
    .upsert({
      phone: phone,
      otp: otp,
      expires_at: new Date(
        Date.now() + 5 * 60 * 1000
      ).toISOString(),
      verified: false
    })
  
  // Send OTP via Fast2SMS
  const response = await fetch(
    'https://www.fast2sms.com/dev/bulkV2',
    {
      method: 'POST',
      headers: {
        'authorization': process.env.FAST2SMS_API_KEY,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        route: 'otp',
        variables_values: otp,
        numbers: phone,
        flash: 0
      })
    }
  )
  
  const data = await response.json()
  
  if (data.return) {
    res.json({ 
      success: true,
      message: 'OTP sent successfully'
    })
  } else {
    res.status(500).json({ 
      error: 'Failed to send OTP' 
    })
  }
})

POST /api/otp/verify

app.post('/api/otp/verify', async (req, res) => {
  const { phone, otp } = req.body
  
  // Get OTP from database
  const { data: otpRecord } = await supabase
    .from('otp_verifications')
    .select('*')
    .eq('phone', phone)
    .eq('verified', false)
    .single()
  
  if (!otpRecord) {
    return res.status(400).json({ 
      error: 'OTP not found. Please resend.' 
    })
  }
  
  // Check expiry
  if (new Date() > new Date(otpRecord.expires_at)) {
    return res.status(400).json({ 
      error: 'OTP expired. Please resend.' 
    })
  }
  
  // Check OTP match
  if (otpRecord.otp !== otp) {
    return res.status(400).json({ 
      error: 'Wrong OTP. Please try again.' 
    })
  }
  
  // Mark as verified
  await supabase
    .from('otp_verifications')
    .update({ verified: true })
    .eq('phone', phone)
  
  res.json({ 
    success: true,
    message: 'Phone verified successfully!' 
  })
})

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DATABASE TABLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create OTP table in Supabase:

CREATE TABLE otp_verifications (
  id UUID DEFAULT gen_random_uuid() 
     PRIMARY KEY,
  phone TEXT NOT NULL,
  otp TEXT NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  verified BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

Add to profiles table:
ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS 
  full_name TEXT;

ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS 
  phone TEXT;

ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS 
  phone_verified BOOLEAN DEFAULT false;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FRONTEND — ONBOARDING COMPONENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Update the onboarding component to 
have 3 steps with progress indicator:

// Progress bar at top:
Step 1 ──●────────── Step 2 ──○────────── Step 3 ──○
Personal Details    Verify Phone      Choose Exam

const [step, setStep] = useState(1)
const [name, setName] = useState('')
const [phone, setPhone] = useState('')
const [otp, setOtp] = useState('')
const [otpSent, setOtpSent] = useState(false)
const [phoneVerified, setPhoneVerified] = 
  useState(false)
const [timer, setTimer] = useState(0)

// Step 1 — Personal Details
if (step === 1) {
  return (
    <div>
      <h2>Welcome to GovtGuru! 🎓</h2>
      <p>Let's set up your profile</p>
      
      {/* Progress */}
      <ProgressBar current={1} total={3} />
      
      {/* Name Input */}
      <label>Full Name *</label>
      <input
        type="text"
        placeholder="Enter your full name"
        value={name}
        onChange={e => setName(e.target.value)}
        minLength={2}
      />
      
      {/* Phone Input */}
      <label>Mobile Number *</label>
      <div style={{ display: 'flex', gap: 8 }}>
        <span>+91</span>
        <input
          type="tel"
          placeholder="10 digit mobile number"
          value={phone}
          onChange={e => {
            const val = e.target.value
                .replace(/\D/g, '')
                .slice(0, 10)
            setPhone(val)
          }}
          maxLength={10}
        />
      </div>
      
      <button 
        onClick={handleSendOTP}
        disabled={
          name.length < 2 || 
          phone.length !== 10
        }
      >
        Send OTP →
      </button>
    </div>
  )
}

// Step 2 — OTP Verification
if (step === 2) {
  return (
    <div>
      <h2>Verify Your Number 📱</h2>
      <p>OTP sent to +91-{phone}</p>
      
      <ProgressBar current={2} total={3} />
      
      {/* 6 digit OTP input boxes */}
      <OTPInput 
        length={6}
        value={otp}
        onChange={setOtp}
      />
      
      <button 
        onClick={handleVerifyOTP}
        disabled={otp.length !== 6}
      >
        Verify OTP ✓
      </button>
      
      {/* Resend Timer */}
      {timer > 0 ? (
        <p>Resend OTP in {timer}s</p>
      ) : (
        <button onClick={handleResendOTP}>
          Resend OTP
        </button>
      )}
      
      <button onClick={() => setStep(1)}>
        ← Change Number
      </button>
    </div>
  )
}

// Step 3 — Exam Selection
// (existing code — just add to step 3)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SAVE TO PROFILE ON COMPLETION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When user completes all 3 steps:

async function completeOnboarding() {
  await supabase
    .from('profiles')
    .upsert({
      user_id: userId,
      full_name: name,
      phone: phone,
      phone_verified: true,
      exam_code: selectedExam,
      exam_date: examDate,
      daily_hours: dailyHours,
      plan: 'free',
      onboarding_completed: true,
      created_at: new Date().toISOString()
    })
  
  // Redirect to dashboard
  router.push('/dashboard')
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESEND OTP WITH TIMER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// Start 30 second countdown
// after OTP is sent:

function startTimer() {
  setTimer(30)
  const interval = setInterval(() => {
    setTimer(prev => {
      if (prev <= 1) {
        clearInterval(interval)
        return 0
      }
      return prev - 1
    })
  }, 1000)
}

// Call startTimer() after 
// successful OTP send

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISPLAY NAME AFTER ONBOARDING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After onboarding is done,
show user's name in:

1. Sidebar bottom:
   [Avatar] Rahul Kumar  ← full_name
            Free Plan

2. Dashboard welcome message:
   "Welcome back, Rahul! 👋"
   Instead of email address

3. Settings page:
   Show full_name in profile section

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPORTANT NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. OTP expires in 5 minutes

2. Maximum 3 OTP attempts per phone
   per hour (prevent abuse)

3. Phone number must be unique
   Two accounts with same phone 
   not allowed

4. If user skips (back button),
   they cannot access the app
   Onboarding is mandatory

5. For Fast2SMS setup:
   - Sign up at fast2sms.com
   - Add API key to Replit Secrets:
     FAST2SMS_API_KEY = your_key
   - Minimum recharge: ₹100
   - ₹0.25 per OTP = 400 OTPs 
     from ₹100

6. Show user's name (not email) 
   everywhere in the app after 
   onboarding

7. Do not break existing 
   onboarding for current users
   who already completed it